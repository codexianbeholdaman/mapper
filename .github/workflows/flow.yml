name: mapper
run-name: also_mapper
on: [push]
jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - name: Nodejs garbage
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
      - run: npm install
      - run: npm t
